Builds
IntermediateFiles
ConfigurationFiles
.project
.config.json
cproject
.externalToolBuilders
*.sdf
*.opensdf
*.opendb
*.suo
*.VC.db
*.vs
*.user
.DS_Store
MiscOldNotes/
xcuserdata/

# Bind-mount target for dev-docker-container-local state (e.g. .vscode-server) - kept out of
# the repo so each container gets its own copy, rather than sharing/bind-mounting from ~ like
# the other dotfiles.
.dockerstuff/

# Local Claude Code settings (per-machine permissions, etc.) - not shared across machines.
.claude/

# Python bytecode cache - the scripts under Tests/Scripts/ are importable as modules (they share a
# dump parser), and importing one writes a __pycache__ beside it.
__pycache__/
*.pyc
