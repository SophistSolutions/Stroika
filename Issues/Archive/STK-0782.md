# STK-782 - ORM features to add

| | |
|---|---|
| status | Open |
| type | Task |
| priority | Medium |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2022-04-09T15:14:33.465-0400 |
| updated | 2022-04-09T15:14:59.758-0400 |

<https://stroika.atlassian.net/browse/STK-782>

## Description

Namespace ORM::Schema {
 
  - Need join
  - Need sub-tables for (aggregated) sets, for case where can be seen by sqlite so can query on elts of the set). For example, query all devices with port 22 open.
o    Maybe use idea like what we did for catcall schema
o    Maybe ‘aggregate’ tables inside tables (schema) – and generate SQL for both in sql generate method. Use concat of names with separator.
o    And probably can autogenerate the ties between the tables (ints auto-created).
o    Maybe case of REFERENCE I don’t need to worry about – consider top level table and user must construct appropriate query?

