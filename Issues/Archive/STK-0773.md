# STK-773 - maybe small bug with synchronized - test and at least add regtest (make sychrionized lock be interruption point).

| | |
|---|---|
| status | Open |
| type | Task |
| priority | Medium |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2022-03-19T12:15:50.549-0400 |
| updated | 2022-03-19T12:15:50.549-0400 |

<https://stroika.atlassian.net/browse/STK-773>

## Description

 
       Synchronized<int> syncObj;
       Thread t [] () {
              auto a = syncObj.GetLock();
       };
       syncObj.lock();
       t.Start();
       Sleep(1);
       t.Abort();    // does that abort the lock – must fix!!!! Add regtest

