# STK-671 - DateTime::Format and Parse () incorrectly handle the format strings %z and %Z (sort of)

| | |
|---|---|
| status | Open |
| type | Bug |
| priority | Medium |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2018-11-07T09:57:12.354-0500 |
| updated | 2021-03-07T16:34:38.016-0500 |

<https://stroika.atlassian.net/browse/STK-671>

## Description

The format strings (timezone related) %z %Z appear only supported in std c++ for 
https://en.cppreference.com/w/cpp/io/manip/put_time
not get_time.
But they both make similar sense. And the support in the current stdc++ lib is unfortunate (for Stroika) because it refers to the global system time (or maybe based on TZ)
Instead, the right thing for the DateTime class - with its PARSE and FORMAT methods, would be to use its associated timezone.
For BOTH Parse() and Format() - this can be done relatively easily by ourselves parsing the format string (%A %B etc) - and for nearly all of them, just calling the time_put/time_get variant to handle the string, but for the %z %Z strings - we handle directly.

## Comments

### Lewis Pringle - 2018-11-07T10:04:12.295-0500

NOTE - I thought of trying to temporarily "SET" the timezone for the format implementation, but there appears to portable (and certainly no per-thread) way todo this (maybe new C++20 time library?) - but even if there were such a way, it wouldnt address parsing out timezone info.

### Lewis Pringle - 2021-03-07T16:34:38.016-0500

now due to refactoring this same defect is in DateTime::QuietParse_

