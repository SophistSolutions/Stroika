# STK-629 - hang during valgrind of Test39 - thread safety

| | |
|---|---|
| status | Resolved |
| type | Bug |
| priority | High |
| resolution | Won't Do |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2017-11-16T12:18:20.221-0500 |
| updated | 2021-12-16T11:21:29.920-0500 |
| resolved | 2021-12-16T11:21:29.863-0500 |

<https://stroika.atlassian.net/browse/STK-629>

## Description

A recent regression. (I think - cuz I've changed testing). Could be due to rewrite of ConditionVariable<>::wait_unti; code.
Issue appears with shutting down threads.
NOTE - ALL the regression tests PASS by themselves. They only fail when run under valgrind.

## Comments

### Lewis Pringle - 2017-11-16T12:25:18.606-0500

testing two possible fixes/workarounds:
#+++ b/Library/Sources/Stroika/Foundation/Execution/ConditionVariable.inl
#@@ -68,6 +68,14 @@ namespace Stroika {
  -                          *  Cannot quit here because we trim time to wait so we can re-check for thread aborting. No need to pay attention to
  -                         *  this timeout value (or any return code) - cuz we re-examine tickcount at the top of the loop.
  -                          */
#+                        {
##+                            // TEST POSSIBLE FIX/WORKAROUND FOR https://stroika.atlassian.net/browse/STK-629
+                            Time:urationSecondsType remaining = timeoutAt - Time::GetTickCount ();
#+                            if (remaining < 0) {
#+                                return cv_status::timeout;
#+                            }
#+                            return std::cv_status::no_timeout; // can be spurious wakeup, or real, no way to know
#+                        }
  -                     }
  -                     else {
  -                         Assert (tmp == std::cv_status::no_timeout);
diff --git a/Library/Sources/Stroika/Foundation/Execution/WaitableEvent.inl b/Library/Sources/Stroika/Foundation/Execution/WaitableEvent.inl
index 480b032fe..7f460f4c1 100644
    - 
      - 
        - a/Library/Sources/Stroika/Foundation/Execution/WaitableEvent.inl
#+++ b/Library/Sources/Stroika/Foundation/Execution/WaitableEvent.inl
#@@ -53,6 +53,8 @@ namespace Stroika {
  -             }
    -             inline bool WaitableEvent::WE_:eekIsSet () const noexcept
  -             {
#+                // TEST POSSIBLE FIX/WORKAROUND FOR https://stroika.atlassian.net/browse/STK-629
#+                typename ConditionVariable<>::QuickLockType critSection{fConditionVariable.fMutex};
  -                 return fTriggered;
  -            }
  -            inline void WaitableEvent::WE_::Set ()

### Lewis Pringle - 2017-11-21T10:19:36.116-0500

WORKED AROUND problem (maybe fixed but must review) with:
// TEST POSSIBLE FIX/WORKAROUND FOR https://stroika.atlassian.net/browse/STK-629
Time:urationSecondsType remaining = timeoutAt - Time::GetTickCount ();
if (remaining < 0) {
return cv_status::timeout;
}
return std::cv_status::no_timeout; // can be spurious wakeup, or real, no way to know
in v2.0a222. Review in next release.

### Lewis Pringle - 2021-12-16T11:21:29.908-0500

I think this was never a bug in Stroika, but just with condition variable checking in (TSAN and) valgrind. Now specific BWA for this.

