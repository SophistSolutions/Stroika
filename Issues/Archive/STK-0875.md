# STK-875 - improve  waitable event thread abort check (change count of how long waiting and wakeup often to check cancelation)

| | |
|---|---|
| status | Open |
| type | Task |
| priority | High |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2022-04-14T18:06:41.362-0400 |
| updated | 2022-04-14T18:06:57.881-0400 |
| labels | v3-Feature |

<https://stroika.atlassian.net/browse/STK-875>

## Description

NOTE - this is related to the CancelationToken project - and may just get fixed by that.
Add more docs on canceleation
o    SEE if there is some other hack which is more efficient – on POSIX (maybe open ANOTHER bug report for this) – to wakeup for abort case using thread cancelation, or anoter q????
 OR – maintain linked list of all ‘waitnig’ condition variables (maybe per-thread) – and add/remove when we wiat on condition variable, and in Thead::Abort () – we check that list and send to it (maybe too costly).
 
https://stackoverflow.com/questions/27341029/what-is-the-best-way-to-wait-on-multiple-condition-variables-in-c11 
 
 
http://man7.org/linux/man-pages/man3/pthread_cancel.3.html
consider using thread cancelation to make ConditionVariable more efficent
 
 
o    FIX DOCS and implement geter/setter or just document to set the global variables???
 
SO I ALREADY KNEW THIS – SHIT – PACKAGE UP WHAT I HAVE HERE AND USE IN re-use in WaitableEvent. When that works – use THAT in BlockingQueue
 
            public:
                /**
  - This API shouldnt be needed - if we had a better underlying implementation, and beware, the API could go away
  - if we find a better way. But callers may find it advisible to control this timeout to tune performance.
                 *
  - The WaitableEvent class internally uses condition_variable::wait_for () - and this doesn't advertise support for
  - EINTR or using Windows SDK 'alertable states' - so its not clear how often it returns to allow checking
  - for aborts. This 'feature' allows us to periodically check. You dont want to check too often, or you
  - effecitvely busy wait, and this checking is ONLY needed for the specail, rare case of thread abort.
                 */
                nonvirtual void SetThreadAbortCheckFrequency (Time::DurationSecondsType frequency);

