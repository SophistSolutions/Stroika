# STK-809 - vswhere instead of looking in hardwired locations in configure

| | |
|---|---|
| status | Open |
| type | Task |
| priority | Low |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2022-04-11T17:21:21.024-0400 |
| updated | 2022-04-11T17:21:21.024-0400 |

<https://stroika.atlassian.net/browse/STK-809>

## Description

create windows only VSWhere (like) script

Get
  Vswhere.exe stuff working – where we pass in INSTALLDIR as param, and have generic scripts for building from that. And VSWhereDir
 
              Then fix configure to use vswhere and the rest from that path.
 
make --silent --directory BootstrapToolsSrc/vswhere whereLatest
 
              ***USE IN GetDefaultToolsBuildDir()
ThirdPartyComponents
 (or build scr tools)
https://github.com/Microsoft/vswhere/releases 
grab vswhere from that
 
Use https://github.com/microsoft/vswhere/releases 
              c:\users\lewis\Downloads\vswhere.exe -all
 
              to identify available (and default?) vs instances so we know where to grab code
              All PLATFORM stuff to specify this additionally as a parameter
 
              "\users\lewis\Downloads\vswhere.exe" -format json
              "\users\lewis\Downloads\vswhere.exe" -format json |grep installationPath
 

takes param optionally saying Visualstudio 2022 or 2019 etc. And then does above or similar logic to what i do now in configure script 

```
 ($PROJECTPLATFORMSUBDIR eq "") {
            my @files = glob("'$PROGRAMFILESDIR/Microsoft Visual Studio/2022/*/VC'");
            if (scalar(@files) != 0) {
                $PROJECTPLATFORMSUBDIR = 'VisualStudio.Net-2022';
```

but then replace all that with a call to this new VSWhere script.

