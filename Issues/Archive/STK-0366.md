# STK-366 - Document design choice with Stroika iterator

| | |
|---|---|
| status | Open |
| type | Task |
| priority | Medium |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2015-10-31T22:53:50.568-0400 |
| updated | 2015-10-31T22:53:50.568-0400 |

<https://stroika.atlassian.net/browse/STK-366>

## Description

•	Document design choice with Stroika iterator.
o	Copy versus Reference
	In Java, they make the choice to reference, and then throw exception when they detect conflict – container change while iterating (check count flag)
	Copying intrinsicly more costly for large objects, but this is often optimizable away (mostly) for pod (e.g. int/pointer) types) .
	Then only cost one we have anyhow – of one virutla  function per NEXT call, and after that very checap and no locking
	And inside virtual REP impl – there we can do the java-style trick of looking at lock counts, but instead of throwing – we smartly retry (or for simplicity just always use locking –up to impl).

