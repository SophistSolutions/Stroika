# STK-856 - small (mostly obsolete but check each point) notes for traceroute sample

| | |
|---|---|
| status | Open |
| type | Task |
| priority | Medium |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2022-04-14T13:40:17.748-0400 |
| updated | 2022-04-14T13:40:17.748-0400 |
| labels | DIFFICULTY-EASY |

<https://stroika.atlassian.net/browse/STK-856>

## Description

TRACEROUTE
Ø  Windows firewall (workaround/set to allow) and unix report error better for not root
Ø  Variant of Traceroute/Ping that does callback – not gather into sequence – so more flexible.
Ø  Traceroute sampling support – maybe show min/max/median/mean instead of each sample.
Ø  WAG at (either do or document in BUG REPORT) – TCP/GRE/UDP support. There is a TCP/UDP echo service I think I can use. See sample traceroute code and see if that’s what it does.
Ø  IPv6 support
Ø  Maybe do same separation of sampling in Traceroute we did for Ping? Maybe objectify Traceroute call? But at any rate – make it work with samples.
 
---
 
>>>> copy this code in Ping as starting point for traceroute and use it to do first draft impl of traceroute.
              >> and do UDP-based ping/tracertoue and TCP, and GRE, etc – see traceroute.c sources. Maybe just open bug report to track this – do quicky icmp based one, and maybe leave rest for later

