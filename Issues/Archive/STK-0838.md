# STK-838 - MAYBE get rid of MakeCommonSerializer() support for InternetAddress, and replace it with more generic support if we can do ToString and T {String()} with concepts

| | |
|---|---|
| status | Open |
| type | Task |
| priority | Medium |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2022-04-14T12:06:00.922-0400 |
| updated | 2022-04-14T12:06:00.922-0400 |

<https://stroika.atlassian.net/browse/STK-838>

## Description

MAYBE get rid of MakeCommonSerializer() support for InternetAddress, and replace it with more generic support if we can do ToString and T {String()} with enable_if.
       (but first add regtest using InternetAddress)
 
       (actually – really want to use – instead of ToString – As<String> ()). That generates natural String format. ToString() is for debugging.

