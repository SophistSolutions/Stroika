# STK-739 - Container constructor calls to inherited must use () not {} - and unclear why

| | |
|---|---|
| status | Resolved |
| type | Task |
| priority | Medium |
| resolution | Done |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2021-06-10T17:38:44.785-0400 |
| updated | 2021-06-10T20:36:49.713-0400 |
| resolved | 2021-06-10T20:36:49.673-0400 |

<https://stroika.atlassian.net/browse/STK-739>

## Description

We get a variety of crazy bugs and/or failure to compile (depending on compiler) using {} instaed of () calls to base Iterable() CTOR in many cases. 
THis happens for all compilers so it must be my misundersnanding, so add ticket to diagnose and better understand.
Some of the problems seemed to be invoking TEMPORARIES (especailly with Sequnce<VariantValue> or Collection<VariantValue>
  -        : inherited{static_cast<inherited&&> (Factory::Mapping_Factory<KEY_TYPE, MAPPED_VALUE_TYPE, KEY_EQUALS_COMPARER>{forward<KEY_EQUALS_COMPARER> (keyEqualsComparer)}())}
and const inherited&, and neioterh worked. Just switchign to () worked.
But its more than tha. Tried

## Comments

### Lewis Pringle - 2021-06-10T18:24:31.012-0400


```
    // Use Iterable<>() to avoid matching Iterable<>(initializer_list<>... - see docs in Iterable::CTORs...
    
```

### Lewis Pringle - 2021-06-10T18:41:42.445-0400

SOLVED (except maybe consider implicaitons a bit about using initializer_list).
  - in a 
member initializer list of a constructor if braced-init-list is used
https://en.cppreference.com/w/cpp/language/list_initialization 

… coupled with:
  - Otherwise, the constructors of T are considered, in two phases:
  - All constructors that take std::initializer_list as the only argument, or as the first argument if the remaining arguments have default values, are examined, and matched by overload resolution against a single argument of type std::initializer_list
  - If the previous stage does not produce a match, all constructors of T participate in overload resolution against the set of arguments that consists of the elements of the braced-init-list, with the restriction that only non-narrowing conversions are allowed. If this stage produces an explicit constructor as the best match for a copy-list-initialization, compilation fails (note, in simple copy-initialization, explicit constructors are not considered at all).

so what is going on is that the initailizer_list<> arg - if it possibly can be - will be used.

MAYBE avoid this by changing VariantValue - to have an EXPLICIT on its conversion of iterable<vairable:value> to variabtvalue. This works badly with anything that might use initializer_lists!

### Lewis Pringle - 2021-06-10T20:36:26.208-0400

also tweaked VariantValue CTORs so this sort of problem is less likely. But still can happen - but close now cuz dont think its happening anymore in Stroika or my samples and I have enough docs to explain whats going on….

