# STK-910 - change ObjectVariantMapper toObjectMapper to instead of taking ptr to object, returns optional of object

| | |
|---|---|
| status | Open |
| type | Task |
| priority | High |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2022-06-21T16:51:59.814-0400 |
| updated | 2022-11-09T20:53:50.306-0500 |
| labels | DIFFICULTY-EASY, v3-Feature |

<https://stroika.atlassian.net/browse/STK-910>

## Description


```
template <typename T>
inline void ObjectVariantMapper::ToObject (const ToObjectMapperType<T>& toObjectMapper, const VariantValue& v, T* into) const
{
    RequireNotNull (into);
    RequireNotNull (toObjectMapper);
    // LOGICALLY require but cannot compare == on std::function! Require (toObjectMapper  == ToObjectMapper<T> ());  // pass it in as optimization, but not change of semantics
    toObjectMapper (*this, v, into);
}

Idea is to ELIMIANTE the need for T to be default constructible. I THINK that fixes the problem.
BREAKING change (slightly) - so be careful adn do as part of v3.
```

## Comments

### Lewis Pringle - 2022-11-09T20:53:50.306-0500

I HOPE this will allow for serializing objects which are not default constructible (even if with custom readers/writers).

