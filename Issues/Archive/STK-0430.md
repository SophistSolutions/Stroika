# STK-430 - Iterable::Select<> produces a new iterable that shaddowns the original, but without upping ref count

| | |
|---|---|
| status | Open |
| type | Bug |
| priority | High |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2016-01-06T15:03:10.699-0500 |
| updated | 2016-02-08T19:13:14.900-0500 |

<https://stroika.atlassian.net/browse/STK-430>

## Description

{
Debug::TraceContextBumper ctx ("{}DbgDump_PerfCounters_:");
DbgTrace (L"(forObj=%s)", forObj.value.c_str ());
//Traversal::Iterable<int>	counterIDs = p.QueryAvailablePerfMetric (serviceContent.perfManager, forObj).Select<int> ([] (const PerfMetricId& i)->int { return i.counterId; });
Sequence<int>	counterIDs = p.QueryAvailablePerfMetric (serviceContent.perfManager, forObj).Select<int> ([] (const PerfMetricId& i)->int { return i.counterId; });
for (PerfCounterInfo pi : p.QueryPerfCounter (serviceContent.perfManager, counterIDs)) {
DbgTrace (L"COUNTER: %s", Characters::ToString (pi).c_str ());
}
}
if I return as Iterable<T> (assign type) - then it asserts out in debug version cuz temporary destroyed while iteraotr still alive (in iterable created with creategenerator);
If I asing directly to Sequence - it gets converted before the temporary is destroyed.
Not sure what todo. As is -its effientt, and mostly OK, but a bit confusing. AT LEAST document ebtter, and maybe chagne.

