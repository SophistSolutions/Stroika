# STK-821 - performance tweak webserver

| | |
|---|---|
| status | Open |
| type | Task |
| priority | Medium |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2022-04-13T06:35:48.306-0400 |
| updated | 2022-04-13T06:35:48.306-0400 |

<https://stroika.atlassian.net/browse/STK-821>

## Description

(note these data maybe quite old/obsolete):
 
Ø  Looking at timing of WTF web service – network traffic in Chrome – looks filesystem read/etc stuff slow.
o    REDO Response.write() stuff so there is a proxy object – outputstream – which if nothing else – MAPS to calling the existing API. So callers CAN use that stream API instead of Response.write
o    Then redo filesystem router code to use Stream::Copy () from the filesystem read file, to the response object)
o    That MAY help a little – but then put in timing code to see where the time is being spent. At least we can start writing while doing reading. That should help some.

