# STK-765 - visual studio .net samples solution rebuild all when ATLMFC not installed produces errors

| | |
|---|---|
| status | Open |
| type | Task |
| priority | Medium |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2022-02-06T21:11:31.023-0500 |
| updated | 2022-02-06T21:11:31.023-0500 |

<https://stroika.atlassian.net/browse/STK-765>

## Description

The Stroika makefiles are smart enuf to not build components like LedIt/LedLineIt/ActiveLedIt that depnd on MFC, if the qFeatureFlag_ATLMFC is none

HOWDEVER, I can see no practical way todo this with the msvc project files

I CAN expose the <PreProcessorDefinitions flags wiht add addition to ApplyCOnfigurations like
  - 
```
  print (OUT "  <PropertyGroup Label=\"UserMacros\" Condition=\"'\$(Configuration)|\$(Platform)'=='$activeConfiguration|Win32'\">\n");
```
  - 
```
  print (OUT "    <PreProcessorDefinitions>$defs2Print</PreProcessorDefinitions>\n");
```
  - 
```
  print (OUT "  </PropertyGroup>\n");
```
  - 
  - 

##### Unclear if/why this would be needed

```
    print (OUT "  <ItemGroup>\n");
    print (OUT "  <BuildMacro Include=\"StroikaBuildToolsExtraPath\">\n");
```
@@ -555,6 +560,9 @@ sub WriteVS2kIntellisenseConfigFile
print (OUT "  <BuildMacro Include="JOBS_FLAG">\n");
print (OUT "     <Value>$(JOBS_FLAG)</Value>\n");
print (OUT "  </BuildMacro>\n");
  - 
```
  print (OUT "  <BuildMacro Include=\"PreProcessorDefinitions\">\n");
```
  - 
```
  print (OUT "     <Value>\$(PreProcessorDefinitions)</Value>\n");
```
  - 
```
  print (OUT "  </BuildMacro>\n");
  print (OUT "  </ItemGroup>\n");
```

and I can do conditionals in .vsproj files as in 
https://docs.microsoft.com/en-us/visualstudio/msbuild/msbuild-conditional-constructs?view=vs-2022 
<Choose> <When Condition="$([System.Text.RegularExpressions.Regex]::IsMatch( $(DefineConstants), '^(.*;)*DEFINED_CONSTANT(;.*)*$'))"> <!-- When DEFINED_CONSTANT is defined. --> </When> <!-- other conditions --> </Choose>

But this doesnt seem to work for Solution files (.sln).
And taths the only place I can find to disable a project (/ dont build it for a conidtionation.

