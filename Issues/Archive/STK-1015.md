# STK-1015 - ObjectVariantMapper: Current serializer/de-serializer API needlessly requires that objects have default CTOR

| | |
|---|---|
| status | Open |
| type | Task |
| priority | Low |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2024-07-16T08:50:02.282-0400 |
| updated | 2024-07-27T10:12:35.215-0400 |

<https://stroika.atlassian.net/browse/STK-1015>

## Description


```
urrent serializer/de-serializer API needlessly requires that objects have default CTOR.
 *
 *              template    <typename CLASS>
 *                  inline  CLASS    ObjectVariantMapper::ToObject (const VariantValue& v) const
 *                  {
 *                      CLASS tmp;
 *                      ToObject (v, &tmp);
 *                      return tmp;
 *                  }
 *
 *                  I THINK - if we redefined the to-mapper - to return a "T" instead of taking a byte* array
 *                  to fill in, we could avoid this issue. However, doing so is NOT possible for the
 *                  automated 'struct' mapper (key). This is its core strategy - to construct an object and
 *                  then fill in (possibly a subset) of its fields. We would need to somehow automate
 *                  calling a particular CTOR instead and that seems tricky given C++'s meager meta-programming
 *                  features.
 *
 *                  But I THINK it would be possible to define the TypeMappingDetails object to take EITHER
 *                  something in the current form, or something using "T" - and map between them. And then
 *                  users of a particular type (e.g. Range()) - could avoid calling the default ctor, and just
 *                  explicitly call the right CTOR.
 *
 *                  Probably relatively LOW priority to fix, however.
 *
 *                  Also - this could cause some issues with the interoperability of To/FromGenericVariantMapper
 *                  and From/ToObjectMapper<T>.
```

## Comments

### Lewis Pringle - 2024-07-16T08:52:02.108-0400

MAYBE instead of ptr to T, add extra level of indirection T**, or better unique_ptr<T>* instead of T*? - so deserializer can allocate the object? Cost overhead? but still allow subclassing.

But not really that simple. Issue is - ToObject code currently just fills in fields and assumes object passed in OK/constructed.
If we add extra indrection (or somehow - like with optional - havent constructed - must construct in first class we run into the call).
But in current system, this is BASE class - not most specific class.

### Lewis Pringle - 2024-07-27T10:11:41.691-0400

note truely fixed, but better documented in the code, and I think added concept cehcks and warnings to make this pretty easy to deal with so lower priority now…

