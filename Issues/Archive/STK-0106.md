# STK-106 - On Linux (gcc & clang) - DbgTrace of very long (>10k) string causes failure - sometimes crash

| | |
|---|---|
| status | Resolved |
| type | Story |
| resolution | Done |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2014-10-08T13:06:33.000-0400 |
| updated | 2015-10-20T21:03:21.005-0400 |
| resolved | 2015-10-20T21:03:20.995-0400 |
| labels | DIFFICULTY-MEDIUM, Soon, WorkingOnNow |

<https://stroika.atlassian.net/browse/STK-106>

## Description

For example, DbgTrace ("responseText =" lines in regression test 39 (IO::Network::Transfer)  - produce large strings, and these display as garbage.
Debugging deeper and doing more DbgTrace calls I ran into frequent crashes.
I thought it was bugs with SmallStackBuffer, or some such. but now. Just this.
Not super critical - but since it can be such a time waster, mark as pretty importnat (not regression I dont believe). NOT clear if its the trace handling or the Format() string code (which would make it a litle more importnat).
In that format code - when trying to repridcue - try making the buffer smalle r(instaead of 10K try 10 bytes and then do resize loop on vsprint()).

## Comments

### Lewis Pringle - 2014-10-08T15:05:15.000-0400

Fixed in v2.0a47. Issue was that we must use va_copy to copy va_list elements, not just assignment/pass by value. Because with vswpr..intf... it munges the value, and if we re-run (cuz buffer not big enuf) - we get crash.

