# STK-976 - alternate Julian Date Algorithms - more precise before gregorian calendar

| | |
|---|---|
| status | Open |
| type | Task |
| priority | Lowest |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2023-02-18T17:19:51.386-0500 |
| updated | 2023-02-18T17:19:51.386-0500 |

<https://stroika.atlassian.net/browse/STK-976>

## Description

As of Stroika v3.0d1, we updated the min/max date requireemnts to go to  apx 4000bc to 9000AD (full julian date range).
But its NOT very accurate before gregorian. Needs work.
Found a bunch of websites with hints, but didnt work through or math didnt work.

NOT IMPRORTANT, but collected notes…

 
        // NOTE LOGIC HERE NOT RIGHT ABOUT ROUNDING
 
#if 0
        /*
  - From https://en.wikipedia.org/wiki/Julian_day:
  - The algorithm is valid for all (possibly proleptic) Gregorian calendar dates
  - after November 23, −4713. Divisions are integer divisions towards zero; fractional parts are ignored.[66]
         *
  - JDN = (1461 × (Y + 4800 + (M − 14)/12))/4 +(367 × (M − 2 − 12 × ((M − 14)/12)))/12 − (3 × ((Y + 4900 + (M - 14)/12)/100))/4 + D − 32075
         */
        auto newAlgo =
            (1461 * (yy + 4800 + (mm - 14) / 12)) / 4
            + (367 * (mm - 2 - 12 * ((mm - 14) / 12))) / 12
  - (3 * ((yy + 4900 + (mm - 14) / 12) / 100)) / 4
            + dd - 32075
            ;
 
 
       
        const year_month_day kStartOfJulianCalendar_ = -4712y / January / 1;    // maybe off by one year due to zero issue
 
 
        {
            const year_month_day testdate = 2023y / February / 18; // maybe off by one year due to zero issue
       
            auto diff = std::chrono::sys_days (testdate) - std::chrono::sys_days (kStartOfJulianCalendar_);
            //auto diff = testdate - kStartOfJulianCalendar_;
            int a = 3;
        }
        {
            // FROM https://en.wikipedia.org/wiki/Julian_day#cite_note-72 
            int y = 4716;
            int v = 3;
            int j = 1401;
            int u = 5;
            int m = 2;
            int s = 153;
            int n = 12;
            int w = 2;
            int r = 4;
            int B = 274277;
            int p = 1461;
            int C = -38;
        }
        {
 
        }
        {
            using namespace chrono;
            ///
            year_month_day ymd (1990y / 01 / 01); // Create a date object. '_y' is a literal like 'f' for float which comes with `date::literals` or `chorno::literals`.
            sys_days total_days = (sys_days)ymd; // Convert it to a time_point.
            total_days += days (44334);          // Add the days.
            year_month_day later = total_days;   // Convert it back to an object.
        }
#endif

