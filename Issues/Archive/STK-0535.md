# STK-535 - fix helgrind regresiosn with Test10_MutlipleThreadsReadingOneUpdateUsingSynchonizedContainer_ (was Remove support for Iterator patching (or make optional))

| | |
|---|---|
| status | Resolved |
| type | Improvement |
| priority | Highest |
| resolution | Done |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2016-09-26T11:59:08.707-0400 |
| updated | 2021-11-10T10:54:58.821-0500 |
| resolved | 2021-11-10T10:54:58.705-0500 |
| attachments | [debug.patch](attachments/STK-0535/debug.patch) |

<https://stroika.atlassian.net/browse/STK-535>

## Description

We CURRENTLY have a new bug (not a regression but a new regression test) - where we get helgrind failures.
regtest 37
Test9_MutlipleThreadsReadingUnsynchonizedContainer_
Test10_MutlipleThreadsReadingOneUpdateUsingSynchonizedContainer_
Discussed with KDJ/Sterling
I think the plan is to make an OPTIONAL param to containers like Sequnece_Array - if iterators are autosafe-patching.
THEN - its the responsabity of the callers to assure iterator acessed syncrhonzed with the owning container (patch but not lcoking other than debug::assertexternalylsynconized)
But most of the stroika code - loses the patching logic. So runs FASTER (and more bug free)
Maybe include in DEBUG mode maintaining the linked list (or something similar) to DETECT bugs and assert out. Maybe on iteartor have a CHANGE COUNT. And then to assert in iterator that they cannot be used after a change to the owning container!
Conssider i = s.begin();
s.append([x]);
i++; // assert ciz s 'changed'.
ALSO - make Remove() method on Sequnce (and maybe others) return an Iterator to next item.

## Comments

### Lewis Pringle - 2016-10-10T20:33:36.008-0400

Alot of efforts to narrow this down and fix it.
Right now - have workaround which is define
#if		qIterationOnCopiedContainer_ThreadSafety_Buggy
and add extra lock around iteration.
ALL you need to repdouce bug is to copy a container and iterator on that copy in one thread while appending in another.
NOT 100% clear if this is a real bug or a quirk of notifying helgrind. Also not clear if I need atomic_thread_fance calls (I added in ifdef/comment in regtest that fails).

### Lewis Pringle - 2017-02-23T15:55:54.317-0500

I wonder if maybe releated to https://stroika.atlassian.net/browse/STK-570
Thats much easier to fix (first) anyhow...

### Lewis Pringle - 2018-08-12T16:56:31.646-0400

I worked on this again. No luck.
I rewrote how we support iterator objects, so they now longer use shared_ptr - they copy direcyly. Not sure that is better or worse performance wise (no measurable difference in my tests). But it didnt help this issue except to eliminate one possible cause of problems.
To reproduce, run
Builds/g++-debug-sanitize_thread_undefined/Test39
valgrind test.
Use the attached patch before building to narrow issue and disable bug workaround. 
[attachment]

### Lewis Pringle - 2021-11-03T10:08:55.816-0400

test if this is fixed - by changes to iteration code (patching).

### Lewis Pringle - 2021-11-10T10:48:07.220-0500

appears fixed as of // APPEARS FIXED AS OF 2021-11-10 - Stroika 2.1b14

### Lewis Pringle - 2021-11-10T10:54:58.792-0500

fixed (probably by getting rid of iterator patcing logic)

