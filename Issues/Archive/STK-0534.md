# STK-534 - Redo Character/String using char32_t - not wchar_t - but preserving ability for c_str to return wchar_t* and not too much cost

| | |
|---|---|
| status | Resolved |
| type | Improvement |
| priority | High |
| resolution | Done |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2016-09-24T10:03:26.499-0400 |
| updated | 2023-01-07T17:07:30.706-0500 |
| resolved | 2023-01-07T17:07:30.628-0500 |

<https://stroika.atlassian.net/browse/STK-534>

## Description

  - @todo   REDO THIS SO ALWAYS USES char32_t - NOT wchar_t!!!! But DONT DO until I have a STRING class
  - implementation based on UTF-8, to minimize the performance costs...
	SUB-type that does the crap about surrogates
	Chagne API so sizeof(charafter) == sizeof(char32). Then fix all asusmpotions about this - all STRING objs return const wchar_t* - and keep that internally. BUT – special case obj MAY also store other encodigns (like ucs4). And then does getlength etc propleryt!!!
	So String.length() NOT necessary same as wsclen(s.c_str())!!!!

## Comments

### Lewis Pringle - 2018-09-27T12:33:53.967-0400

Some ideas on how to do this:
o Could store each char* (char32_t* etc) values in the envelope, so can see the const/non-const of the methods. Non-const methods would then FREE the extra pointers and follow C++ safety rules.
o Maybe better - store char8, char16, and char32* pointers in REP itself. Make it impl detail which is stores as 'base copy'.  MAYBE use one big buffer for all these strings, and just have pointers that point into it? For small strings this would work well. For larger, they would need to be malloced. Because the c_str() style methods are all const, this means we'd need a LOCK around read/write these cached pointers (costly). Probably a single global lock - otherwise might be too big? Or maybe SpinLock (small and fast).

### Lewis Pringle - 2018-09-27T12:35:56.902-0400

Would then have:
const wchar_t* c_str ();
const char* c8_str ();
const char16_t c16_str ();
const char32_t* c32_str ();
So we would need 3 ptrs.

### Lewis Pringle - 2023-01-07T17:07:30.693-0500

TONS of work for this and lots releated - still some related todo, but this part is done.

