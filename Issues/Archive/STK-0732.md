# STK-732 - Support ETags directly within FileSystemREquestHandler, so we can more efficiently handle conditional gets

| | |
|---|---|
| status | Open |
| type | Task |
| priority | Low |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2021-03-14T20:22:23.719-0400 |
| updated | 2021-03-14T20:22:23.918-0400 |

<https://stroika.atlassian.net/browse/STK-732>

## Description

this already works FULLY. (Conditional gets and ETags).
But we may also want to emit (based on user configuraiton) Last-Modified, and handle conditional if modified since, and supprot etags based on a file watcher (maybe not needed) or just doing a file access to get the date from a file.
This is what thingks like nginx do (I think).
This wont affect what goes over the wire, but just the efficientcy of respoding to filesystem requests (avoiding the read fom file, digest, and aborted networkgin write).

