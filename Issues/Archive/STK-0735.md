# STK-735 - openssl wrapper - provide support for call to legacy provider (for openssl3 support)

| | |
|---|---|
| status | Resolved |
| type | Task |
| priority | Lowest |
| resolution | Done |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2021-05-05T21:04:54.906-0400 |
| updated | 2021-05-12T12:58:44.548-0400 |
| resolved | 2021-05-12T12:58:44.393-0400 |

<https://stroika.atlassian.net/browse/STK-735>

## Description

see https://wiki.openssl.org/index.php/OpenSSL_3.0
OSSL_PROVIDER *legacy;
OSSL_PROVIDER *deflt;
/* Load Multiple providers into the default (NULL) library context */
legacy = OSSL_PROVIDER_load(NULL, "legacy");
if (legacy == NULL) {
printf("Failed to load Legacy provider\n");
exit(EXIT_FAILURE);
}
deflt = OSSL_PROVIDER_load(NULL, "default");
if (deflt == NULL) {
printf("Failed to load Default provider\n");
OSSL_PROVIDER_unload(legacy);
exit(EXIT_FAILURE);
}
/* Rest of application */
OSSL_PROVIDER_unload(legacy);
OSSL_PROVIDER_unload(deflt);
exit(EXIT_SUCCESS);

## Comments

### Lewis Pringle - 2021-05-12T12:58:44.501-0400

this probbakly could be done better, since we dont really manage lib contexts much, but you can call this load function and it probably will mostly owrk...  Just try to do before creating threads etc - and updating default context sb fine. 
@todo managing other contexts and assigning them to threads etc...

