# STK-59 - XMLDB (like I have in HealthFrame)

| | |
|---|---|
| status | Open |
| type | Story |
| priority | Medium |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2014-07-13T20:55:37.000-0400 |
| updated | 2016-02-08T19:15:13.519-0500 |

<https://stroika.atlassian.net/browse/STK-59>

## Description

xmldb
•	Main ideas are NON-Relational DB 
o	Hopefully good replacement for folks using ORM, not replacement for use of sql useage
o	Takes XMLSchema to define DB layout
o	For each type – create folder, and for each instance, separate file
	At least one back-end impl would do this – idea is multiple virtual backend impl eingnes – one that might back end to storing on relational db via sql, etc.
o	Objects have opaque ids automatically, and can be referenced (even if not in the xml schema)
o	Must support transactions (objects)
o	Eventually support XML Schema validation, but hard to use COTS validator given disparate file model – but not impossible – given sax parsing validators. Just that would be very very costly (or I need a clever trick – like isolating TRANSACTIOSN and just validating THEM somehow)
•	API COULD be 2 tiered
o	Map-based (like ADO) – Records are assocations of fieldname -> memory::variant
o	And have second API based on this, that generates C++ structs from schema, and automatically maps to that map / assoc list API.
o	Then EXAMPLE
	 User u;
•	U.x = ‘3’;
•	Db.Add (U);

