# STK-750 - why cannot declare IO::Network::UniformResourceIdentification::{Host,Authority} and URI as noexcept copy CTOR copy assign operator=

| | |
|---|---|
| status | Open |
| type | Task |
| priority | Lowest |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2021-11-24T17:14:08.753-0500 |
| updated | 2021-11-24T17:27:06.155-0500 |

<https://stroika.atlassian.net/browse/STK-750>

## Comments

### Lewis Pringle - 2021-11-24T17:15:28.395-0500


```
     *        noexcept - unclear why I cannot declare copy constructor and copy assignment operators as noexect
     *        on GCC. THIS compiles fine, but then later bits of code that use it fail to compile (g++ 9 at least).
```

```
    Host (const Host&)     = default;
    Host (Host&&) noexcept = default;

public:
    nonvirtual Host& operator= (const Host&) = default;
    nonvirtual Host& operator= (Host&&) noexcept = default;
```

### Lewis Pringle - 2021-11-24T17:26:37.489-0500

workaround needed on URI, Host and Authority classes (losing noexcept on copy CTOR and copy assignment operator). So far only verified we need to do that lose on gcc 9 - so COULD be a compiler bug.

