# STK-320 - demo of containers

| | |
|---|---|
| status | Resolved |
| type | Improvement |
| priority | Low |
| resolution | Done |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2015-10-31T10:34:52.531-0400 |
| updated | 2021-11-03T14:44:46.147-0400 |
| resolved | 2021-11-03T14:44:46.111-0400 |

<https://stroika.atlassian.net/browse/STK-320>

## Description

SOON TODO
writeup stroika container demo app
(rename SimpleSequenceUsage to SimpleContainerUsage)
(and add SimpleStringUsage)
Include use of regexp in that test case too!
one file for each major contianer type
void CHECK_TRUE_(bool t)
{
.. verufy tryeregfakess of debug flag
}
eg:
demo_sequence()
{
Sequence<int> f;
f.push_back (3);
vector<int> a = f;
CHECK_TRUE (a.length () == 1) and a[0] == 3)
CHECK_TRUE (f == a);//not sure about last compare)- woirks?
}
Void testAppends()
{
Auto appender  =[] (Sequence<int>* s) -> void
{
for (int i = 1; I < 10000; I ++) {
s->append ;
}
}
Auto directAccessor  =[] (Sequence<int>* s) -> void
{
// qsort – but need right kind of accessor!
// stroika iterators don’t work
// so do cheap one now
// inline bubble sort 
}
Sequence<int> defaultS;
Sequence<int> linkedlistS = Sequence_DoublyLinkedList<int> ();
Sequence<int>  arrayS = Sequence_Array<int> ();
// insert timers
Appender(&defaultS);
Appender(&linkedListS);
Appender(&arrayS);
directAccessor  (&defaultS);
directAccessor  (&linkedListS);
Ap directAccessor  pender(&arrayS);
// but now try the stroika way..
{
Sequence<int> s = Sequence_DoublyLinkedList<int> ();
Appender(&s);
S = Sequence_Array<int> (s);
directAccessor  (&s);
}
... and so on...

