# STK-745 - DoublyLinkedList/LinkedList would be faster for some use cases if we tracked LENGTH

| | |
|---|---|
| status | Open |
| type | Task |
| priority | Low |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2021-11-03T14:48:37.851-0400 |
| updated | 2021-11-03T14:48:37.851-0400 |

<https://stroika.atlassian.net/browse/STK-745>

## Description

in particular, the current regression tests call GetLength () on the Sequence_LinkedList<>. Thats quite slow. It MIGHT make sense to (optionally) cache the lenght.
With newer C++ features (if constexpr, enable_if , [[maybezerosized__attr]] etc) - I think we can add a field OPTIONALLY for the length and conditionally use/ update it. This optioanl could be propagated to a template param in Sequence_DoublyLinkedList::CTOR, not sure exactly how. We want to select teh rep based on this. BUt the rest of the data is non-constant.
LOWPRI.

