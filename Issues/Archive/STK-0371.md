# STK-371 - String::c_str() returns OBJECT

| | |
|---|---|
| status | Open |
| type | Task |
| priority | Low |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2015-10-31T23:01:39.905-0400 |
| updated | 2015-10-31T23:01:39.905-0400 |

<https://stroika.atlassian.net/browse/STK-371>

## Description

•	Think about STACK_CSTR_OBJ class
o	Struct STACK_CSTR{ 
	Operator const char_t* () const;
	CTOR?DTOR
	Wchar_t buf[32];
	};
	Like ‘small StackBuffer’ – except copyable, and allocates PTR if string arg > 32 in length
	THEN – have
•	String::c_str() return STACK_CSTR instead of c_str(). Only issue I can see with that is use in Format (“%s”, foo.c_str()) – DO THINK will work – cuz doesn’t know what to return…

