# STK-210 - add http://api.jquery.com/jquery.grep/ to stroika

| | |
|---|---|
| status | Open |
| type | Improvement |
| priority | Medium |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2015-10-25T00:56:38.000-0400 |
| updated | 2015-10-26T10:30:11.618-0400 |

<https://stroika.atlassian.net/browse/STK-210>

## Description

o	Add http://api.jquery.com/jquery.grep/ to stroika
	Containers
	I THINK cannot go in iterable becase return type SB right for each container. Maybe do in iterable, but ‘hide’ in each subclass (like Set<>). That’s probably best.
	Maybe with new c++ rules for inheritance return type subtyping this make work with a single virtual function!
	But either way – have it return a subset of the ‘this’ argument container that passes the argument lambda function.
	Unclear if it should be called ‘grap’ or ‘filter’ or ‘subset’. Anyhow – in doccoment include all these names.

