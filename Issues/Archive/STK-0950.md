# STK-950 - Improved validation of DateTime::Parse, and perhaps fix to datetime parse with + timezone

| | |
|---|---|
| status | Resolved |
| type | Task |
| priority | Medium |
| resolution | Done |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2022-11-05T11:30:45.237-0400 |
| updated | 2023-07-19T03:37:14.283-0400 |
| resolved | 2023-07-19T03:37:14.166-0400 |

<https://stroika.atlassian.net/browse/STK-950>

## Description

{    // TODO DEBUGGING
            // To capture issues Sterling showed me 2022-11-04
            try {
                DateTime dt = DateTime::Parse (L"1906-05-12 12:00:00+00", DateTime::kISO8601Format);
                VerifyTestResult (false);
            }
            catch (const DateTime::FormatException&) {
                // good
            }
            catch (...) {
                VerifyTestResult (false);
            }
            try {
                DateTime dt = DateTime::Parse (L"1906-05-12T12:00:00+00", DateTime::kISO8601Format);
                VerifyTestResult ((dt.GetDate () == Date{Year(1906), MonthOfYear::eMay, DayOfMonth::e12}));
                VerifyTestResult ((dt.GetTimeOfDay () == TimeOfDay {0, 0, 0}));
                VerifyTestResult (dt.GetTimezone ()->GetBiasFromUTC (dt.GetDate (), *dt.GetTimeOfDay ()) == 0);
            }
            catch (...) {
                VerifyTestResult (false);
            }
         }

## Comments

### Lewis Pringle - 2022-11-06T19:19:52.062-0500

Fixed this for DateTime, but @todo - same logic added for Date::Parse and TimeOfDay::Parse()

### Lewis Pringle - 2023-07-19T03:37:14.228-0400

re-reviewed and appears complete as of v3.0d1 (maybe could use more testing but not that important).

