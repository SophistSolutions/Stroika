# STK-871 - add configuration mechanism for better defaults for stopping timeout (and maybe others) for ServiceManager

| | |
|---|---|
| status | Open |
| type | Task |
| priority | Medium |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2022-04-14T17:26:17.395-0400 |
| updated | 2022-04-14T17:26:17.395-0400 |

<https://stroika.atlassian.net/browse/STK-871>

## Description

 
 
fSERVUCE MGR – provide a CONFIGURATION the servicemgr gets – that says what the default timeout is for ‘stopping’ and use that. Maybe also change cmd args to not use default values for Stop () – maybe allow you to specifiy, but overload not default arg – so the default can come from the configured setting
 
           constexpr Time::DurationSecondsType kTimeOut_{30.0}; // a vaguely reasonable default - apps can override by handling before calling Run

