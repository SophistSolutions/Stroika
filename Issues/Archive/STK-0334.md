# STK-334 - TextOutputStream::UnSeekable_UTF8_IRep_() should store codecvt_utf8 in a data member or possibly global variable - performance (const re-usable/threadsafe)

| | |
|---|---|
| status | Open |
| type | Improvement |
| priority | Medium |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2015-10-31T19:10:28.210-0400 |
| updated | 2015-10-31T19:10:28.210-0400 |

<https://stroika.atlassian.net/browse/STK-334>

## Description

TextOutputStream::UnSeekable_UTF8_IRep_() should store codecvt_utf8 in a data member or possibly global variable (if that turns out to be safe – do research).
But cosntrucitng them seems somewhat more costly than I realized. So at least don’t done one per write, but one per writer object instance.

