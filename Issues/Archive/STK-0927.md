# STK-927 - Redo alot of Stroika thread use with intervalTimer class

| | |
|---|---|
| status | Open |
| type | Task |
| priority | Medium |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2022-07-29T09:28:52.319-0400 |
| updated | 2022-10-21T08:54:33.012-0400 |

<https://stroika.atlassian.net/browse/STK-927>

## Description

things like the SSDP listener, etc, could probably be better done with the interval timer code? Stuff were it just needs to wakeup once in a while (time based) on and not wait on objects (notify) etc. Maybe not. Maybe not so useful, but look through and see what can be done that way.

## Comments

### Lewis Pringle - 2022-10-21T08:54:33.012-0400

maybe replace other threads in Logger with use of IntervalTimer and just a dependency that you must startup interval timer before logger mgr. OR – allow nested starters? And have logMgr silently start it. That might be tter for stuff that can be started without options.
 
       MAYBE pattern for module starters is no-arg starters/acitivators can be done nested, but ones with OPTIONS MUST be done first!
       @todo carefully consider how nested activators would work – take optional arg to other activator so can be suppressed? Or allow nesting (but how to handle both specifying options – nested activators cannot specify them? Or first wins or last wins?).

