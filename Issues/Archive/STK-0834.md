# STK-834 - Misc notes to review about condition variables and waitable events

| | |
|---|---|
| status | Open |
| type | Task |
| priority | Medium |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2022-04-13T12:34:14.802-0400 |
| updated | 2022-04-14T13:16:29.090-0400 |

<https://stroika.atlassian.net/browse/STK-834>

## Description

----
 
https://stackoverflow.com/questions/15657637/condition-variables-c-net 
 
 
Look for example use of blocking queue  - and test – but probably stop using waitable event. Maybe use condition variable, or review. DOCUMENT why safe to use wiatable event. It seems to just guarnateee waking up one thread, not all.
       >>>>> MAYBE DONE
 
       >>> DOCUMENT MY “WAIT” on “AutoReset” event doesn’t guarantee ALL threads awakened and for manual reset, does guarantee all awkened, but not necessarily before any manual reset. So Auto-RESET DANGEROUS IF MULTIPLE THREADS WAITING – well – could be OK – but beware of this issue, and use condition_variable directly if you want to assure all threads awakend (so maybe I should just do that – but then need to implement q to assure all awakened???)
 
----
 
 
              See if I should be using condition variables – not waitable even – in ThreadPool code.
 
 
---
 
Windows AutoReset waitable evnet – MAY – guarantee to wakeup one thread. I’m not sure mine does. But make clear, autoreset is for when only about one thread to twakeup, and use manual reset if you want to wake them all up.
              See https://blog.mikejmcguire.com/2014/12/22/using-the-autoresetevent-class-to-wake-multiple-threads-in-c/ 
 
              >>> REEXAMINE qExecution_WaitableEvent_SupportWaitForMultipleObjects
       And condition variables.
 
 
Ø  Look at EXAMPLE in
http://en.cppreference.com/w/cpp/thread/condition_variable/wait 
 
 
 
 
              >>> CONSIDER LOSE ResetType, but instead, have Set-WakeAll, Set-And-Auto-Reset-First-To_Wake? Or name eAutoReset to eAutoReset1?
 
 
 
>>>>> SHIT – MY ONE USE OF AUTORESET EVENTS MAYBE REASON FOR LONGSTANDING BLOCK BUG WHERE SEEMED WE MAYBE RAN OUT OF THREADS – OR CLOSE. WE USE AUTOWAKE EVENT in THREADPOOL CODE, and so not ALL threads wake. Not SURE, but if two finish at same time, maybe only only one wakes? No – maybe not serious – but could cuase some delays in threads waking….

## Comments

### Lewis Pringle - 2022-04-14T13:16:29.090-0400

MORE notes to review:

Maybe hypbrid of WaitableEvent and ConditionVarHelper – which adds to CondtionVarHelper a function<> object that is quivilent to the ‘fTriggerd’ flag with the WaitableEvent. Then you just construct this object (passing in right tester) and you can ‘wait’ reliably. Not that different from regular condition_var  - except you specify the condition tester ONCE in one place – not at every wait location. Maybe call ConditionVariableEvent?
 
 
---
              Use example to illustrate why Stroika simpler – when using stuff like conditionvar/blocking queue – don’t need to worry about shuytdown case (at least don’t have to handle explicitly – the defauilt logic takes care of it automatically – except for DTOR/noexcept issue and with finally even that.
 
 
----
 
----
 
       bool WaitableEvent::WE_::WaitUntilQuietly (Time::DurationSecondsType timeoutAt)
 
       WaitQuietyly should return cv_status::timeout or cv_status::no_timeout
 
----
 
 
qConditionVariablesSafeInAsyncSignalHanlders
       MOVE TO STDLIB OR OTHER PRIOVATE CONFIG FILE… SOMEPLACE ELSE…

