# STK-798 - review docs and thread safety of SharedByValue

| | |
|---|---|
| status | Resolved |
| type | Task |
| priority | Medium |
| resolution | Done |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2022-04-11T09:43:21.425-0400 |
| updated | 2026-03-10T08:39:38.204-0400 |
| resolved | 2026-03-10T08:39:38.149-0400 |

<https://stroika.atlassian.net/browse/STK-798>

## Description

              GREATLY IMPROVE DOCS ON SharedByValue
 
Ø  EXPLAIN CLONE method does NOT need to be threadsafe, because SharedByValue must be EXTERANLLY synchronized (the container) – and the CONTAINEE can ONLY be MODIFIED if refCount > 1. So if 2 threads accessing it, its through 2 envelopes and therefore both are readers.
Ø  BOTH CAN decide to BREAKREFERENCES at the same time, and that needs to be written carefully to be threadsafe (assure it is). MAYBE that requires atomic_load of shared_ptr, so refcounts assured right?
o   Key to review – isAssureOnceRefence correct ??? or is it a race
 
I THINK I SEE FLAW:
              THREAD1:
Ø  Assure1Refefnce – it’s the only one, so it just returns and the user starts modifying sole copy
THREAD2:
Ø  At same time but a tick after Assure1Refernce check – copies the SharedByValue() so it has a refCount of 2, but Thread1 still running and modifying!
Ø  User in thread 2 can look at object while thread1 is still finishing its modification.. To fix – we need Thread1 to hold lock whole time its modifying?
Ø  Maybe that makes SharedByValue unwise? But central to Stroika containers/strings etc..

## Comments

### Lewis Pringle - 2022-04-11T09:46:11.691-0400

PROBABLY just update docs so clear AssureNOrFewerReferences () only works if all the referfneces held by the calling thread (so always works for cease of 1) and works for cease of 2 if the caller owns the 2 references).

