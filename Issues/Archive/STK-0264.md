# STK-264 - cleanup delete (directory or filename) to Filesystem object

| | |
|---|---|
| status | Open |
| type | Improvement |
| priority | Medium |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2015-10-29T09:30:10.852-0400 |
| updated | 2021-12-16T11:37:00.606-0500 |

<https://stroika.atlassian.net/browse/STK-264>

## Description

>>> CLEANUP – add CDELETE (directory or filename) to FileSystem object!
#if 1
::system (Characters::Format (L"rm -rf %s", toPath_ (volumeID, folderName).c_str ()).AsNarrowSDKString ().c_str ());
#else
IO::FileSystem:irectory (toPath_ (volumeID, folderName)).AssureDeleted ();
#endif
if (fileName.IsMissing ()) {
IO::FileSystem:irectory (toPath_ (volumeID, folderName)).AssureDeleted ();
}
else {
// @todo - fix to use stroika API - so we get throw if fails
(void)::unlink (toPath_ (volumeID, folderName, *fileName).AsSDKString ().c_str ());
}

