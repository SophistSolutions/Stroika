# STK-58 - State Machine Framework

| | |
|---|---|
| status | Open |
| type | Story |
| priority | Lowest |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2014-07-13T20:54:13.000-0400 |
| updated | 2015-10-20T08:47:56.518-0400 |
| labels | Frameworks::* |

<https://stroika.atlassian.net/browse/STK-58>

## Description

STATE MACHINE LIBRARY:??
MAYBE USEFUL FOR SOME STUFF – LIKE COMMS (acu-fpga):
enum class State_ {
S1, S2, S3….
};
static  Set< State >          kUsefulStateSet1;	// used in example code for calls to WaitUntilStateIn() – 
static  Set< State >          kUsefulStateSet2;
static  Set< State >          kUsefulStateSet3;
atomic< State >               fState;
mutable Event                           fStateChangedEvent_;
nonvirtual  void    UpdateState_ (State_ newS);
nonvirtual  void    WaitUntilStateIn_ (Set< State _> acceptableStates, Time:urationSecondsType timeout = Time::kInfinite) const;
virtual (or parameterized) utility to validate what are legal state transitions.
Maybe use Callback Objects – for when state transitions happen? Or let them wait on the event object? Or maybe register  callbacskk for a set of ‘enteirng’ states’)
The utility would be to hide the thread safe managing of callbacks etc – template on STATE_TYPE.

