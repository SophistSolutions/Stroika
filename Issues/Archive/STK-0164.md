# STK-164 - debug why (overloads issue string/variantvalue)

| | |
|---|---|
| status | Open |
| type | Improvement |
| priority | High |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2015-10-21T09:26:09.262-0400 |
| updated | 2016-09-24T10:01:56.702-0400 |

<https://stroika.atlassian.net/browse/STK-164>

## Description

Debug why:
Set<String>    featureStrings;
for (VariantValue vv : features->As<Sequence<VariantValue>> ()) {
featureStrings.Add (vv.As<String> ());
}
featureStrings.Remove (L"FanARemappedToSolenoidPowerCal");
featureStrings.Remove (L"FanBRemappedToSolenoidWavelengthCal");
topLevelObjMap.Add (L"SupportedFeatures", VariantValue (Sequence<VariantValue> (featureStrings)));
last nline no compile??!!! – temp wrong type??? At least on gcc
((NOTE:
WORKAROUND IS:
#if 1
Sequence<VariantValue>  vv;
for (auto i : featureStrings) {
vv.Append (VariantValue );
}
#else
topLevelObjMap.Add (L"SupportedFeatures", VariantValue (Sequence<VariantValue> (featureStrings)));
#endif

