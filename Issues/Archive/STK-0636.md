# STK-636 - Enhance Timezone class to support real timezones (e.g. American/NewYork) and handle all the cases of mapping offsets (applying all teh rules for differnt dates)

| | |
|---|---|
| status | Open |
| type | Task |
| priority | Low |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2018-01-20T11:33:36.554-0500 |
| updated | 2018-11-07T09:44:42.149-0500 |

<https://stroika.atlassian.net/browse/STK-636>

## Description

I think this is working correctly for 'localtime' which is what matters most. I think that's all thats provided by the builtin standard time code in locales.
But it would be nice to be able to construct a timezone object for an arbitrary timezone, and have that work too.
REview TimeZoneInformationType  code. It maybe needs to be enahnced to include the rules from the TZDatabase.
Maybe use tzdatabase (somehow compiled/parsed). Maybe find stdc way of doing it. Maybe use boost. But somehow make it easy to say Timezone tz= “America/NewYork” or some such.
	Look at using http://www.boost.org/doc/libs/1_52_0/libs/locale/doc/html/dates_times_timezones.html boost::locale::calendar - to 
I read the code that comes with the TZDatabase. It looks like everthing is there to do what I want, but its a mess, and appears hardwired to work for the current timezone from an environment variable. I would need to significantly adapt that code to make it work in stroika for abitrary handed in tz.
Maybe thats what ICU or boost does?
Another note on this:
*
  - @todo   MAYBE use http://www.iana.org/time-zones/repository/releases/tzdata2015b.tar.gz and code - and have part of
  - thirdpartylibs, and then compile the zone file there, and reference that somehow here? Or add something
  - to that code to generate our own table which we can #include? Maybe add BuildTools (like Tools) - and
  - there no dependnecies on stroika (execept makefile part) - and build code which we use to boostrap stroika
  - building file in intermediatefiles which we #incldue as initailzied data for timezone module
AND - when I do this - add checks to Test_9_TZOffsetAndDaylightSavingsTime_ () also use explicit timezones.

## Comments

### Lewis Pringle - 2018-10-30T10:43:00.403-0400

NOTE - C++20 will have some better timezone support, including the global named tiemzone database - I think.

