# STK-872 - generically properly handle EINTR

| | |
|---|---|
| status | Open |
| type | Task |
| priority | High |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2022-04-14T17:31:33.669-0400 |
| updated | 2022-04-14T17:31:48.170-0400 |

<https://stroika.atlassian.net/browse/STK-872>

## Description

FileOutputStream::Write () must handle EINTR (check docs) – and have a CheckForThreadInteruption () and document its an interruption point --- SAME for FileInpoutStream!
virtual void Write (const Byte* start, const Byte* end) override
    {
        Require (start != nullptr or start == end);
        Require (end != nullptr or start == end);
 
        if (start != end) {
            lock_guard<const AssertExternallySynchronizedLock> critSec{*this};
 
            const Byte* i = start;
            while (i < end) {
                try {
#if qPlatform_Windows
                    int n = Execution::ThrowErrNoIfNegative (_write (fFD_, i, Math::PinToMaxForType<unsigned int> (end - i)));
#else

BUT this is not really sufficient. ANYPLACE we call ThrowPOSIXErrNoIfNegative or ThrowPOSIXErrNo - we should look carefully. If the erorr is EINTR, do CheckForInterruption and if we get past it, we cannot treat as success OR exxception, but as a RETRY. HERE - and probably hundreds of other places we need to add that logic. Then can ASSERT in this routine (maybe) we dont get EINTR - cuz must be handled first another way (I think I ahve a procedure that takes lambda funtion and retries - must use that in places like above and do the then this can never get EINTR).

