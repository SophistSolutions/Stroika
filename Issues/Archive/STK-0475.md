# STK-475 - Implement New Thread::GetConfigurationStatus () API - on any targets we can

| | |
|---|---|
| status | Open |
| type | Feature |
| priority | Medium |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2016-04-06T09:20:29.963-0400 |
| updated | 2016-04-06T09:24:07.589-0400 |

<https://stroika.atlassian.net/browse/STK-475>

## Description

One hint on MAYBE how todo this on windows would be:
another HINT on windows side - to detect stack size and usage:
public static class StackManagement
{
[StructLayout(LayoutKind.Sequential)]
struct MEMORY_BASIC_INFORMATION
{ public UIntPtr BaseAddress; public UIntPtr AllocationBase; public uint AllocationProtect; public UIntPtr RegionSize; public uint State; public uint Protect; public uint Type; }
;
private const long STACK_RESERVED_SPACE = 4096 * 16;
public unsafe static bool CheckForSufficientStack(UInt64 bytes)
{ MEMORY_BASIC_INFORMATION stackInfo = new MEMORY_BASIC_INFORMATION(); UIntPtr currentAddr = new UIntPtr(&stackInfo); VirtualQuery(currentAddr, ref stackInfo, sizeof(MEMORY_BASIC_INFORMATION)); UInt64 stackBytesLeft = currentAddr.ToUInt64() - stackInfo.AllocationBase.ToUInt64(); return stackBytesLeft > (bytes + STACK_RESERVED_SPACE); }
[DllImport("kernel32.dll")]
private static extern int VirtualQuery(UIntPtr lpAddress, ref MEMORY_BASIC_INFORMATION lpBuffer, int dwLength);
}
With pThread, I think mcuh of it we maybe able to get - by using pthread_getattr_np http://man7.org/linux/man-pages/man3/pthread_getattr_np.3.html, and then pthread_attr_getstack etc/...

