# STK-453 - Add BLOB to VariantValue

| | |
|---|---|
| status | Resolved |
| type | Improvement |
| priority | Medium |
| resolution | Done |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2016-02-11T11:25:39.656-0500 |
| updated | 2022-04-09T11:24:12.656-0400 |
| resolved | 2022-04-09T11:24:12.440-0400 |

<https://stroika.atlassian.net/browse/STK-453>

## Description

Not 100% sure about this.
Maybe a good idea.
COULD MAYBE automatically map this to/from base64 in JSON reader/writer. Maybe in writer we convert to base64. Reader reads back a string. But then if we say "As<BLOB>()) it gets converted from string (base64) to blob?
Maybe have 'converter' property on JSON reader/writer to map String to/from 'binary'/ BLOB?

## Comments

### Lewis Pringle - 2016-02-11T13:11:33.008-0500

return String::FromAscii (Cryptography::Encoding::Algorithm::EncodeBase64 (Memory::BLOB (data, data + len)));

### Lewis Pringle - 2022-04-09T11:22:23.742-0400

One question - is how to convert a BLOB to JSON. This appears nearly uniformly always (google search) to be represented as base64. So that seems a pretty solid answer.
But then the next question is - how do you tell if its a string or base64 when reading a string. Answer is you cannot. But the good news is - doesn’t matter. Same as with dates etc.
Just have a VariantValue of type BLOB that WHEN converted to a string (as it is by JSON writer code) - its converted to base64 string. And comparisons with strings have to be smart (as if both translated to BLOB and compared or maybe translated to string but then maybe case ambiguity - but maybe not - so lets say transalted to string and compared).
And if you say AsBLOB (like AsDate) - it tries to convert at that point. I THINK this will work fine.

### Lewis Pringle - 2022-04-09T11:24:12.642-0400

Done along time ago - just noticed.

