# STK-352 - old threading idea - kept to make sure nothing useful

| | |
|---|---|
| status | Open |
| type | Task |
| priority | Lowest |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2015-10-31T21:20:14.136-0400 |
| updated | 2015-10-31T21:20:14.136-0400 |

<https://stroika.atlassian.net/browse/STK-352>

## Description

	Add somewhere in TODO – that we can have extra ‘thread locking policy’ traits fields in the concrete containers (like bag_linkedlist<>…. So can somehow be assigned to regular bag_linked_list,(not sure how cuz different traits object?).. but doesn’t affect API
o	Idea is maybe we can chose between
	Normal locks
	Optimistic locking
	Assume used from one thread (but in debug builds validates this claim)
•	Call this AssumeExternallySynchonized
	Bag<int> b = Bag_LinkedList<Bag_LinkedList_Traits_assume_one_thread<int>> (); 
o	Note someplace – TENTATIVE way to handle (below) optimistic locking stuff – is to do CONCRETE_TRAITS as EXTRA template param for concrete types (like Bag_LinkedList<T, TRAITS (really bagtraits), Bag_LinkedList_IMPL_TRAITS>
	EXPLAIN WHY THIS APPROACH
•	Otherwise – if we embedded impl choices in BASE TRAITS – then assignemtnt to base Bag<T,TRAITS> wouldn’t work right. 
•

