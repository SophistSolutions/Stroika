# STK-981 - TemporaryString class

| | |
|---|---|
| status | Open |
| type | Task |
| priority | Medium |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2023-07-21T12:00:21.344-0400 |
| updated | 2023-07-21T12:05:15.612-0400 |

<https://stroika.atlassian.net/browse/STK-981>

## Description


```
Temporary string.\	
	
	Class TemporaryString {
		shared_ptr<DATA>;
		REP has WEAKPTR to that data.
	So when temporary string goes away, weakptr KNOWS.
		STILL BAD IDEA cuz semantics in that case terrible. But its possible todo if worth while performance wise, because at least in debug buils any failures detectable.

	To be clear – ONLY USE THE SHARED{TR in DEBUG builds. In release builds do nothing. SharedPtr thing just trick to VERIFY the string rep isn’t be stored anyplace. Also for that to work – must make TemporaryString not copyable and not moveable etc.

```

```
struct TmpString : String {
#if qDebug
shared_ptr<char32_t[]>  data;
weak_ptr<char32_t[]> weakdata;
#endif
TmpString(char32_t data[]) 
  String (make rep from string constent (dataa)
{
  
}
TmpString() = delete;
TmpString(const TmpString&) = delete;
operator=(const TmpString) = delete;

};
```

Trick here is since TmpString not over copied/copyable, when it goes out of scope ,just assert weakptr == nullptr in its DTOR. So assert nobody hung onto pointer. That means - String rep no longer used anywhere

