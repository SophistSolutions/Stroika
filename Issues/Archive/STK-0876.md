# STK-876 - possibly lose WebServices::GetWebServiceArgsAsVariantValue

| | |
|---|---|
| status | Open |
| type | Task |
| priority | Medium |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2022-04-14T18:09:52.334-0400 |
| updated | 2022-04-14T18:09:52.334-0400 |

<https://stroika.atlassian.net/browse/STK-876>

## Description

CONSIDER if we should lose WebServices….
 
                             GetWebServiceArgsAsVariantValue
 
Seems to be about the same as
 
PickoutParamValues () – but aggregates all into master Variant object. Maybe that’s better, but do we need both apis?
 
IF we need both, more consistent names, and document why, but probably lose one or the other.
 
              PROGRESS:
                             GetWebServiceArgsAsVariantValue () is ONLY called internally in WebService\Server\VariantValue* so easy to see if it can be replaced

