# STK-852 - cleanup const usage for smart ptrs in a few places

| | |
|---|---|
| status | Open |
| type | Task |
| priority | Medium |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2022-04-14T13:33:06.640-0400 |
| updated | 2022-04-14T13:33:06.640-0400 |
| labels | DIFFICULTY-EASY |

<https://stroika.atlassian.net/browse/STK-852>

## Description

----
       ReVIEW but probably change all methods on ConnectionOrinetedetc sockets to CONST – since they modify the PTR dereferenced data not the smart ptr itself. And we typically want to declare const Ptr& p to avoid bumping refcount needlessly.
Then fix stuff like:
                       nonvirtual void onConnect_ (ConnectionOrientedSocket::Ptr s);
 
       >>> if we do, note as warning, const on smartptr unrelated to const on underlying object. Those objects are never known to be const (though individual methods in the rep can be const). Note sure how to document that from a docs point of view.
 
 
    >>> happy with const choice, but confusing that ConnectionOrientedMasterSocket inherits from Socket::Ptr instead of Socket (like I do for Stream)
 
<<<< INCOMPLETE, BUT COMPLETE ENUF TO RELEASE, AND FINSIH IN NEXT RELEASE… just shared_rep stuff and a few other minor things  - getref() etc – to cleanup – and maybe a bit more)

