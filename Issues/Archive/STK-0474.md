# STK-474 - Support new Thread::Configuration parameter for creating threads on all platforms - set stack size

| | |
|---|---|
| status | Open |
| type | Feature |
| priority | Low |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2016-04-06T08:53:07.010-0400 |
| updated | 2016-04-06T08:56:27.052-0400 |

<https://stroika.atlassian.net/browse/STK-474>

## Description

For now - this comprises just specifying the stack size and the stack guardsize.
For AIX I see only one obvious way to set this (given use of the std::thread class which uses all teh defaults):
// According to https://www.ibm.com/support/knowledgecenter/ssw_aix_61/com.ibm.aix.performance/thread_supp_tun_params.htm
// Default: 98 304 bytes for 32-bit applications, 196 608 bytes for 64-bit applications.
// Range: Decimal integer values from 0 to 268 435 455 which will be rounded up to the nearest page (currently 4 096).
if (sizeof (void*) == 4)
{
static const char kStackSizeEnvVarSetting_[] = "AIXTHREAD_STK=130000";
DbgTrace ("Setting env-var %s", kStackSizeEnvVarSetting_);
and same for AIXTHREAD_GUARD..... (or some such).
The trouble with this is its a bit of race - you modify global variables (setenv). Best to avoid if we can.
For Linux - I'm not sure.
UNIX/Linux:
gthread_create (_gthread_t threadid, void (func) (void),void *_args)
{ return gthrw(pthread_create) (_threadid, NULL, __func, __args)
so construcint std::thread wtih libstdc++ just uses the default for posix_threads.
MAYBE I can replace (link trick) - the default gthread_create function?
For windows - I'm also not sure how todo this. I'm not sure is imporant,a nd since I dont really know how to do it - back burtner for now.

