# STK-1026 - Mapping Remove(iterator) broken - used in retainall - qCompilerAndStdLib_stdhashmap_erase_Buggy???

| | |
|---|---|
| status | Resolved |
| type | Task |
| priority | Low |
| resolution | Won't Do |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2025-04-01T13:38:02.591-0400 |
| updated | 2025-04-03T11:40:02.670-0400 |
| resolved | 2025-04-03T11:40:02.564-0400 |

<https://stroika.atlassian.net/browse/STK-1026>

## Description

related to https://stroika.atlassian.net/browse/STK-539 
triggered by Mapping<KEY_TYPE, MAPPED_VALUE_TYPE>::RetainAll (const ITERABLE_OF_KEY_TYPE& items)
code - small case and Foundation_Containers_Mapping, RetainAllCaseFails_

## Comments

### Lewis Pringle - 2025-04-03T11:38:11.262-0400

To debug

```
diff --git a/.vscode/launch.json b/.vscode/launch.json
index 99a6bc0cab..43ab915ede 100755
--- a/.vscode/launch.json
+++ b/.vscode/launch.json
@@ -15,7 +15,7 @@
       "name": "(gdb-UNIX) Launch EXE",
       "type": "cppdbg",
       "request": "launch",
-      "program": "${workspaceFolder}/Builds/${command:cpptools.activeConfigName}/Tests/Test37",
+      "program": "${workspaceFolder}/Builds/${command:cpptools.activeConfigName}/Tests/Test16",
       //"program": "${workspaceFolder}/Builds/${command:cpptools.activeConfigName}/Samples-SystemPerformanceClient/SystemPerformanceClient",
       "args": ["-m"],
       "stopAtEntry": true,
diff --git a/Library/Sources/Stroika/Foundation/Containers/Mapping.inl b/Library/Sources/Stroika/Foundation/Containers/Mapping.inl
index 97f32b5c39..adde8a0054 100644
--- a/Library/Sources/Stroika/Foundation/Containers/Mapping.inl
+++ b/Library/Sources/Stroika/Foundation/Containers/Mapping.inl
@@ -7,8 +7,14 @@
  ***************************** Implementation Details ***************************
  ********************************************************************************
  */
-#include "Stroika/Foundation/Containers/Factory/Mapping_Factory.h"
+
+ #include <iostream>
+
+
+ #include "Stroika/Foundation/Characters/ToString.h"
+ #include "Stroika/Foundation/Containers/Factory/Mapping_Factory.h"
 #include "Stroika/Foundation/Debug/Assertions.h"
+#include "Stroika/Foundation/Debug/Trace.h"
 #include "Stroika/Foundation/Debug/Cast.h"
 #include "Stroika/Foundation/Traversal/Generator.h"
 
@@ -330,6 +336,12 @@ namespace Stroika::Foundation::Containers {
          */
         size_t thisSize = this->size ();
 
+        if constexpr (same_as<string,KEY_TYPE>) {
+            cerr << "DEBUG CALL TO RETAINALL" << endl;
+            cerr << "\titems=" << Characters::ToString(items) << endl;
+            cerr << "\t*this=" << Characters::ToString(*this) << endl;
+        }
+
         // Small case - just remove a few items
         bool fewerLookupsThanAdds = items.size () <= thisSize;
 
@@ -346,12 +358,27 @@ namespace Stroika::Foundation::Containers {
 #if qStroika_Foundation_Debug_AssertionsChecked
                     [[maybe_unused]] size_t sz = this->size ();
 #endif
-                    i = this->erase (i);
+
+if constexpr (same_as<string,KEY_TYPE>) {
+    cerr << "\tERASING - I BEFORE=" << i->fKey << endl;;
+}
+i = this->erase (i);
+if constexpr (same_as<string,KEY_TYPE>) {
+    if (i.Done()) {
+        cerr << "\tERASING - I DONE" << endl;;
+    }
+    else {
+        cerr << "\tERASING - I AFTER=" << i->fKey << endl;;
+    }
+}
 #if qStroika_Foundation_Debug_AssertionsChecked
                     Assert (this->size () == sz - 1u);
 #endif
                 }
                 else {
+                    if constexpr (same_as<string,KEY_TYPE>) {
+                        cerr << "\tSKIPPING - CUZ OK - I =" << i->fKey << endl;;
+                    }
                     ++i;
                 }
             }
diff --git a/Tests/16/Test.cpp b/Tests/16/Test.cpp
index b37ab7c501..bb04c2ab40 100644
--- a/Tests/16/Test.cpp
+++ b/Tests/16/Test.cpp
@@ -315,8 +315,8 @@ namespace {
     {
         // See https://stroika.atlassian.net/browse/STK-1026 - and undo hack in RetainAll for smallCase
         // This test IS failing on MACOS XCODE 15.3 (and others)
-        Mapping<string, int> mOrig{{"id", 1}, {"y", 7}, {"x", 7}};
-        Mapping<string, int> m = mOrig;
+        Mapping_stdhashmap<string, int> mOrig{{"id", 1}, {"y", 7}, {"x", 7}};
+        Mapping_stdhashmap<string, int> m = mOrig;
         m.RetainAll (initializer_list<string>{"id"});
         EXPECT_TRUE (m.Keys ().SetEquals (initializer_list<string>{"id"}));
     }

DEBUG CALL TO RETAINALL
        items=[ id ]
        *this=[ {x: 7}, {y: 7}, {id: 1} ]
        ERASING - I BEFORE=x
        ERASING - I AFTER=y
        ERASING - I BEFORE=y
        ERASING - I AFTER=x
        ERASING - I BEFORE=x
        ERASING - I DONE
        
        
So - it appears the mapping_stdhasmap code (directing calling/re=isoig the mappingerase api)
seems to screw up somehow. See if its just this version of clang++, and maybe ignore/workaround that way.

```

### Lewis Pringle - 2025-04-03T11:40:02.635-0400

https://stackoverflow.com/questions/79551148/how-to-copy-stdunordered-map-while-preserving-its-order 

My misunderstadning. This is not a bug (but a poor stl design choice). Workaround by REMOVING the containers that dpeend on unorered associative containers from stl. Instead do my own Hashtable impl

