# STK-149 - Improve ToString (especially Optional)

| | |
|---|---|
| status | Closed |
| type | Task |
| priority | Medium |
| resolution | Done |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2015-10-20T22:20:19.960-0400 |
| updated | 2016-12-17T13:21:35.220-0500 |
| resolved | 2016-12-17T13:21:34.058-0500 |

<https://stroika.atlassian.net/browse/STK-149>

## Description

SEE ABOUT ToString() code – SHOULD do about what I want.make suie that workls
MAYBE just need pair<> and KeyValuePair<> support?
THEN use ToString() overloads in Analitiqa code (to test)

## Comments

### Lewis Pringle - 2015-10-20T22:20:50.293-0400

Consider tool like DbgTraceDumper module
+// Debugging utilities
+namespace {
+    void    Dump_ (const Set<pid_t>& ps, const String& prefix = String ())
+    {
+        Debug::TraceContextBumper ctx ("{}:ump_(Set<pid_t>)");
+        StringBuilder sb;
+        sb += L"{ ";
+        for (auto p : ps) {
+            sb += Characters::Format (L"%d, ", p);
+        }
+        sb += L" }";
+        DbgTrace (L"%s%s", prefix.c_str (), sb.c_str ());
+    }
+}
+namespace {
+    void    Dump_ (const PerProcessStaticStats_& p, const String& prefix = String ())
+    {
+        DbgTrace (L"%sPerProcessStaticStats_ { fProcessName: %s; fProcessStartedAt: %s; fCmdLine: %s;  fParentProcessID: %d;  fExecutablePath: %s; } ", prefix.c_str (), p.fProcessName.c_str (), p.fProcessStartedAt.Format ().c_str (), p.fCmdLine.Value ().c_str (), p.fParentProcessID.Value (-1), p.fExecutablePath.c_str ());
+    }
+    void    Dump_ (const Mapping<pid_t, PerProcessStaticStats_>& ps, const String& prefix = String ())
+    {
+        Debug::TraceContextBumper ctx ("{}:ump_(Mapping<pid_t,PerProcessStaticStats_>)");
+        if (not prefix.empty ()) {
+            DbgTrace (L"%s", prefix.c_str ());
+        }
+        for (auto p : ps) {
+            Dump_ (p.fValue, Characters::Format (L"%d: ", p.fKey));
+        }
+    }
+}
+namespace { 
Use probably templates or iterable. Handy. Maybe “ToString” – as helper – just for debugging?

### Lewis Pringle - 2016-12-17T13:21:35.208-0500

This was mostly done along time ago, but closed out cuz I finally got this working with optional

