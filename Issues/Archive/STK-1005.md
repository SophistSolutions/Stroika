# STK-1005 - Need mechanism to run ApplyConfigurations after building third-party-components, to fetch its pkgconfig files

| | |
|---|---|
| status | Resolved |
| type | Task |
| priority | Medium |
| resolution | Done |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2024-05-31T09:38:36.983-0400 |
| updated | 2025-03-13T09:48:18.472-0400 |
| resolved | 2025-03-13T09:48:18.300-0400 |

<https://stroika.atlassian.net/browse/STK-1005>

## Description


```
        # # WORKAROUND til we have https://stroika.atlassian.net/browse/STK-1005 addressed
        if (index ($PkgConfigNames, "libxml-2.0") != -1) {
            $newCfgObj .= " , ";
            my $element = trim (`realpath --canonicalize-missing Builds/$activeConfiguration/ThirdPartyComponents/include/libxml2`) . "/";
            $element = trim ("`cygpath --mixed --path \"$element\"`");
            $newCfgObj .= "\\\"$element\\\"";
        }

```

## Comments

### Lewis Pringle - 2024-05-31T10:01:25.508-0400

when done - we can lose 

```
            # WORKAROUND til we have https://stroika.atlassian.net/browse/STK-1005 addressed
            # # Though included in packageconfig support(Builds\Debug\ThirdPartyComponents\lib\pkgconfig\libxml-2.0.pc), in a queer place,
            # # and if we count on it, intellisense/vscode wont know right directories
            push @INCLUDES_PATH, trim (`realpath --canonicalize-missing Builds/$configurationName/ThirdPartyComponents/include/libxml2`) . "/";
            push @CPPFLAGS, '-DLIBXML_STATIC';  # cannot use pkg-config for this because then wouldn't be seen in .vscode etc

```
issue there is that the pkgconfig file for libxml2 has this #define entry and weird -I location for includes. Must duplicate that  in ApplyConfigurations) for now…

### Lewis Pringle - 2025-03-12T12:18:35.246-0400

made some progress 2025-03-12 - but still not fully there - lost this test for .mk file, but still issue with vscode files.

### Lewis Pringle - 2025-03-12T21:24:57.962-0400

[v3-Dev 4153437c44] mostly fixed https://stroika.atlassian.net/browse/STK-1005  - Configuration.mk depends on .rc files; and fixed ApplyConfigurations to check pkg-config files

