# STK-604 - Consider mechanism to automatically prepend 'while' clauses to exception messages.

| | |
|---|---|
| status | Resolved |
| type | Improvement |
| priority | High |
| resolution | Done |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2017-07-13T15:01:07.126-0400 |
| updated | 2021-12-16T11:23:02.502-0500 |
| resolved | 2021-12-16T11:23:02.415-0500 |

<https://stroika.atlassian.net/browse/STK-604>

## Description

Consider some more generic mechanism to handle this common case:
#if qPlatform_Windows
try {
ThrowErrNoIfNegative<Socket:latformNativeHandle> (::bind (sfd, (sockaddr*)&useSockAddr, sizeof (useSockAddr)));
}
catch (const Execution:latform::Windows::Exception& e) {
if (e == WSAEACCES) {
Throw (StringException (L"Cannot Bind to port: WSAEACCES (probably already bound with SO_EXCLUSIVEADDRUSE)"));
}
}
#else
// EACCESS reproted as FileAccessException - which is crazy confusing.
// @todo - find a better way, but for now remap this...
try {
ThrowErrNoIfNegative (Handle_ErrNoResultInterruption ([&sfd, &useSockAddr]() -> int { return ::bind (sfd, (sockaddr*)&useSockAddr, sizeof (useSockAddr)); }));
}
catch (const IO::FileAccessException&) {
Throw (StringException (L"Cannot Bind to port: EACCESS (probably already bound with SO_EXCLUSIVEADDRUSE)"));
}
#endif
catch (...) {
Throw (StringException (L"Cannot Bind to port: " + Characters::ToString (current_exception ())));
}
—
This could then replace the mechaniusm we use to add filenames to file access excetpions.
Someething MAYBE like:

## Comments

### Lewis Pringle - 2017-07-13T15:04:40.040-0400

Declare class Execution::WhileDoing ctx  { L"opening file fred" };
This would set a thread local variable (really somehow linked list of them) - but needs to be cheap.
Then - while doing any exception (StringException class) - we grab all the TLS stored WhileDoing objects and copy them into excetion being thrown.
Then overide (automatically) the message generated:
"While WHILE_MSG"  + " " + BASIC_EXCEPTION_MSG;
Only modest cost - and probably pretty good results on message generation?

### Lewis Pringle - 2021-12-16T11:23:02.485-0500

Implemented this probably more than a year ago - Activity  mechanism now does this.

