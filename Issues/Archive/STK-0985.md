# STK-985 - VariantValue operator <=> is slow for mappings

| | |
|---|---|
| status | Open |
| type | Task |
| priority | Low |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2023-10-11T16:35:35.648-0400 |
| updated | 2023-10-11T16:38:55.457-0400 |

<https://stroika.atlassian.net/browse/STK-985>

## Description

Core issue is just with mappings.
to compare - they need to be sorted.
Maybe a little more complex for == compare, versus 3way. For 3way they need to be sorted.
COULD keep Mappings always as SortedMapping<>. Or could OPTIONALLY take SOrtedMapping<String,VariantValue> and treat at as another type. EITHER WAY, could support As<SortedMapping<String,VariantValue>> - and use that to compare, and just be more efficient sometimes if we already store the sorted mapping. Not sure who/when that sortedmapping would get stored though, so not sure thats a good plan.

