# STK-238 - Test_WString2UTF8_win32API  verusus codecvt_utf8 cleanup?

| | |
|---|---|
| status | Open |
| type | Improvement |
| priority | Medium |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2015-10-27T22:21:43.012-0400 |
| updated | 2015-10-27T22:21:43.012-0400 |

<https://stroika.atlassian.net/browse/STK-238>

## Description

>>> see if we can easily replace atomic_lock stuff with DebugAssertCheckerMutex, and then see iof anyting rbeaks. Maybe add ifdef to control this during experiemtns and eventually transiation.
---
First redo 
st   codecvt_utf8<wchar_t>   kConverter_;        // safe to keep static because only read-only const methods used
constexpr wchar_t kS1_ [] = L"asdbf asdkfja sdflkja ls;dkfja s;ldkfj aslkd;fj alksdfj alskdfj aslk;df;j as;lkdfj aslk;dfj asl;dkfj asdf";
constexpr wchar_t kS2_ [] = L"z\u00df\u6c34\U0001d10b";
void    Test_WString2UTF8_win32API (const wchar_t* s, const wchar_t* e)
{
string      tmp;
WideStringToNarrow (s, e, kCodePage_UTF8, &tmp);
}
void    Test_WString2UTF8_codecvt_utf8 (const wchar_t* s, const wchar_t* e)
{
const wchar_t*  sc  =   s;
const wchar_t*  ec  =   e;
std::string tmp((e - s) * kConverter_.max_length(), '\0');
const wchar_t* from_nex
uysing smallstackbuffer, and see if its any part mem alloc speed tweek or if its all codecovt that’s is slower than windows call
Write portable utility function so we can alays call 
const   codecvt_utf8<wchar_t>   kConverter_;
(OR windows function) to do mapping
---
#if     qPlatform_Windows
Tester (
L"UTF82WString win32API vs codecvt_utf8",
Test_UTF82WString_win32API, L"win32API",
Test_UTF82WString_codecvt_utf8, L"codecvt_utf8",
1390000,
2.1,
&failedTests
);
#endif
#if     qPlatform_Windows && 0
Tester (
L"WString2UTF8 win32API vs codecvt_utf8",
Test_WString2UTF8_win32API, L"win32API",
Test_WString2UTF8_codecvt_utf8, L"codecvt_utf8",
1390000,
2.1,
&failedTests
);
Performnance test – but I think we can switch most internal code to using this (or at least not the mSFT code)
#if     !qCompilerAndStdLib_string_conversions_Buggy
namespace {
const   codecvt_utf8<wchar_t>   kConverter_;		// safe to keep static because only read-only const methods used
}
#endif

