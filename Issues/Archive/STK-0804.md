# STK-804 - make easier to run sample test cases from top level makefile (as done in regression tests)

| | |
|---|---|
| status | Open |
| type | Task |
| priority | Low |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2022-04-11T16:48:48.514-0400 |
| updated | 2022-04-11T16:55:04.599-0400 |

<https://stroika.atlassian.net/browse/STK-804>

## Description

Find a way to redo regression tests ‘Running SAMPLE_APP basic tests’
       In such a way that you can do make run-sample-tests, and INJECT around it the
       Valgrind testing code? So the list of actual tests in one place and easier to run them from cmd line without all the regressiontest stuff
       (maybe some sort of ‘PREFIX var arg to that script where you stick the valgrind magic)\\

## Comments

### Lewis Pringle - 2022-04-11T16:55:04.599-0400

       Add (someplace) make test-samples, and have that run basic tests of what samples CAN be tested. MAYBE write something in python to exercise samples? Or just in sh. Then include that in regression tests. Maybe include it in make tests (have make foundation-tests do what make tests does now and also make sample-tests do sample tests and make tests do both?) – no not quite – but something like this.

