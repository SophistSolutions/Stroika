# STK-642 - New approach to comparison objects, and support this - for now - just in Set<> and SortableSet<> and similarly for Mapping<> and maybe others?

| | |
|---|---|
| status | Resolved |
| type | Task |
| priority | Medium |
| resolution | Done |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2018-03-07T09:26:59.494-0500 |
| updated | 2018-04-14T19:49:59.714-0400 |
| resolved | 2018-04-14T19:49:59.625-0400 |

<https://stroika.atlassian.net/browse/STK-642>

## Description

Seriously consider Set<T> having
	Change EqualsComparer to EqualComparerType (function<bool(T,T)>) and DefaultEqualsComparer
	Review implications for inherited methods from Iterable<>
	Review implications for sorted comparisns (subclasses sortedset).
	Review implications for commutatitivty)
	Consider if I can do this ONLY for Set<>/SortedSet<> - just as a way to get started, and if works well, do for Mapping etc.
	Do all work in backend. Just extra param to CTOR. Then backend does the rest. Not sure we want to bother with TRAITS for Set<>. Simpler without, and the only thing used to replace is comparer, and that’s better replaced by CTOR ARG.
USE
http://en.cppreference.com/w/cpp/utility/functional/equal_to
instead of my usual compare crap
and http://en.cppreference.com/w/cpp/utility/functional/less
and I THIUNK – test this
using EqualsComparerType  std::function<bool(const T&,const T&)>;
void WebService::Server::ExpectedMethod (const Request* request, const Set<String>& methods, const Optional<String>& fromInMessage)
{
String method{request->GetHTTPMethod ()};
// @todo rewrite to do better case insensitive compare
Iterable<String> lcMethods = methods.Select<String> ([](const String& s) { return s.ToLowerCase ();  });
if (not methods.Contains (method.ToLowerCase ())) {
Execution::Throw (
Execution::StringException (
Characters::Format (L"Got HTTP method %s%s, but expected one from %s", Characters::ToString (method).c_str (), (fromInMessage ? (L" from " + *fromInMessage) : L"").c_str (), Characters::ToString (methods).c_str ())
));
}
}

## Comments

### Lewis Pringle - 2018-04-14T19:49:59.677-0400

Major change to handling comparers. MOSTLY - switch to using stdc++ approach, but added 'traits' mechanism to idientify classes of compares for automatic conversion. Redidi ALL containers (we support) to take argument altnaratente comperes, and lost TRAITS from about all of them.
How just traits on CTORs, where we tkae by value arg of comparer (optionally).

