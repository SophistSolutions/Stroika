# STK-566 - ToString (....) support, and cleanup use of templates

| | |
|---|---|
| status | Resolved |
| type | Improvement |
| priority | Medium |
| resolution | Done |
| reporter | Lewis Pringle |
| assignee | Lewis Pringle |
| created | 2017-01-21T23:49:36.284-0500 |
| updated | 2023-11-21T14:37:13.417-0500 |
| resolved | 2023-11-21T14:37:13.377-0500 |

<https://stroika.atlassian.net/browse/STK-566>

## Description

template    <typename T, typename  =  typename enable_if<has_ToString<T>::value, T>::type>
inline  String  ToString_ (const T& t)
{
return t.ToString ();
}
template    <typename T, typename  =  typename enable_if<has_beginenditerable<T>::value and not has_ToString<T>::value and not is_convertible<T, String>::value, T>::type>
String  ToString_ (const T& t)
{
StringBuilder sb;
sb << L"[";
bool didFirst { false };
for (auto i : t) {
if (didFirst) {
sb << L", ";
}
else {
sb << L" ";
}
sb << ToString ;
didFirst = true;
}
if (didFirst) {
sb << L" ";
}
sb << L"]";
return sb.str ();
}
template    <typename T, typename  =  typename enable_if<is_convertible<T, String>::value, T>::type>
inline  String  ToString_ (const T& t)
////
moving typename stuff to from overloads to typename area didnt work - dont udnerstand why.
---
and doing
template    <typename T, typename... ARGS>
inline  String  ToString (const T& t, ARGS&& ... args)
{
return Private_::ToString_ (t);
}
generates no errors, but generates link errors (
...
unresolved external symbol "class Stroika::Foundation::Characters::String __cdecl Stroika::Foundation::Characters::ToString<class Stroika::Foundation::Containers::Set<class Stro
(even if fixing repeat of template declaration in Threads.h)

## Comments

### Lewis Pringle - 2017-02-23T15:18:25.280-0500

Tried again - and same error on gcc as with visual studio.

### Lewis Pringle - 2017-02-23T15:20:46.335-0500

I patched BOTH teh declaration and template <typename T, typename... ARGS>
inline String ToString (const T& t, ARGS&& ... args)
{ return Private_::ToString_ (t); }definitions in ToString.h (
AND the one in Therad.h for threadid (seems red-herring).

### Lewis Pringle - 2017-02-23T16:26:43.937-0500

maybe I need to PARTIAL specailize variadic template ... so
template <typename... ARGS>
String ToString (const bool& t, ARGS&& ... args); /???

