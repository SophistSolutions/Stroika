QCLI_APP_ROOT_API=http://localhost:8084

# Optional build-time fallback, used by src/boot/configuration.ts only when
# neither /config.json nor QCLI_APP_ROOT_API supplies one. Left unset here, so
# the final fallback (window.location.port) applies.
#QCLI_APP_DEFAULT_API_PORT=8080
